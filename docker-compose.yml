version: '3.9'
services:
  web:
    build: 
      context: ./
      dockerfile: Dockerfile
    ports:
      - "7015:80"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
  react:
    image: react
    build: 
      context: /front
      dockerfile: DockerFile
    ports:
      - "3000:3000"